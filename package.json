{
  "name": "shannonfi",
  "version": "0.1.0",
  "description": "Shannon's Demon vault on Solana",
  "private": true,
  "workspaces": [
    "programs/*"
  ],
  "scripts": {
    "test": "anchor test",
    "build": "anchor build",
    "deploy:localnet": "anchor deploy --provider.cluster localnet",
    "deploy:devnet": "anchor deploy --provider.cluster devnet"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@project-serum/anchor": "^0.30.1",
    "@solana/web3.js": "^1.91.0",
    "@jup-ag/api": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-mocha": "^10.0.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.10"
  }
}
